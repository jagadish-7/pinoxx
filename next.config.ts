import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: false, // Disabled to allow certain non-strict behaviors, re-rendering issue with usePathname in AuthHeader
};

export default nextConfig;
